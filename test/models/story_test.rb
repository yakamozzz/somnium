require 'test_helper'

class StoryTest < ActiveSupport::TestCase
  # validates :title, :text, presence: true,  at least one test for each of your validations 
  # and at least one test for every method in your model.

   test "the truth" do
     assert true
   end

end
